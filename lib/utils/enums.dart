
enum CurrentScreen{
  ItemsScreen,
  MyItemsScreen,
}
enum ChatMessageType { text, audio, image, file }

enum ConfirmDelete { CANCEL, YES }

enum ScrollType{ScrollUp, ScrollDown}

enum AuthPage{LoginPage, RegistrationPage}

enum MessageFrom{
  Sender,
  Receiver,
}